# 数学建模 C 题问题4交接文档

## 1. 交接范围

本次工作在问题2、问题3现有代码基础上进行增量修改，完成了问题4的动态电价优化，未重新实现问题2、问题3的核心算法。

已完成的主要内容：

1. 仅读取附件2、附件3、附件4，建立严格输入白名单；
2. 分析0:00、6:00、12:00、18:00四类光伏预测残差；
3. 完成问题4-2“周期感知预测 + 动态电价 + 储能优化”；
4. 完成问题4-3“多阶段光伏预测 + 动态电价 + MPC滚动修正”；
5. 生成并填写 `result4-2.xlsx`、`result4-3.xlsx`；
6. 完成物理约束、时间对齐、数据泄漏和Excel完整性检查；
7. 输出分析报告、CSV数据和SVG图像。

计算结果覆盖2025年2月1日至2025年12月31日，共334天、每天144个10分钟时段。

## 2. 主要文件

### 2.1 核心代码

- `problem4_optimization.py`：问题4主程序、数据读取、残差分析、A/B/C策略编排、审计、Excel及报告输出；
- `problem4_plotting.py`：无第三方绘图库依赖的SVG绘图后端；
- `problem2_optimization.py`：增加了静态144点电价和全年动态电价矩阵的兼容接口；
- `problem3_optimization.py`：增加了滚动MPC逐日使用附件4动态电价的能力；
- `microgrid_optimization.py`：公共调度、MILP、实时执行、Excel模板写入和物理审计函数。

### 2.2 测试

- `tests/test_problem4_dynamic.py`：附件白名单、残差定义、动态电价接口和逐日电价结算测试；
- `tests/test_problem4_plotting.py`：无 `matplotlib` 环境下的SVG绘图回归测试；
- 其余测试文件用于验证问题1—3和公共储能调度逻辑未发生回归。

### 2.3 输出

- `result4-2.xlsx`：问题4-2正式结果；
- `result4-3.xlsx`：问题4-3正式结果；
- `problem4_analysis_report.md`：论文可用的结果汇总；
- `problem4_analysis.json`：完整模型指标、残差统计和审计结果；
- `problem4_strategy_comparison.csv`：A/B/C策略比较数据；
- `problem4_pv_residual_statistics.csv`：光伏残差统计；
- `problem4_figures/`：残差时序、ACF、策略费用、储能动作和SOC图。

## 3. 输入数据与白名单

问题4主程序只允许读取：

- `附件/附件2.xlsx`：实际负荷和实际光伏；
- `附件/附件3.xlsx`：0:00、6:00、12:00、18:00发布的未来24小时光伏预测；
- `附件/附件4.xlsx`：全年逐日动态电价。

问题4不读取附件1，也不读取任何历史 `result*.xlsx` 作为优化输入。结果模板只在写出阶段从 `附件/附件5/` 复制。

程序会检查：

- 附件2负荷和光伏均为 `365×144`；
- 附件3为 `365×4×24`；
- 附件4为 `365×144`；
- 日期必须连续覆盖2025年全年；
- 发布时刻必须依次为0、6、12、18点；
- 所有负荷、光伏、预测和电价必须有限且非负。

## 4. 时间对齐口径

本题原始Excel使用区间终点作为列标签，最终统一映射为对应物理区间，不循环移动数值：

- 原始列 `0:10` 对应 `0:00-0:10`；
- 原始列 `0:20` 对应 `0:10-0:20`；
- 原始列 `0:00(+1)` 对应 `23:50-24:00`；
- 每天严格保留144个10分钟时段。

附件3整点预测采用线性插值转换为10分钟粒度。预测数组中的“未来1小时”对应发布时间后的第一个整点终点。插值起点只使用发布时刻已经观测到的光伏值：

- 0:00使用前一日最后一个时段的光伏值作为锚点；
- 6:00、12:00、18:00使用刚结束时段的实际光伏作为锚点；
- 发布时间之后的实际光伏不进入预测和优化。

功率单位为kW，电量单位为kWh，转换关系为：

\[
E_t=P_t\times\frac{1}{6}.
\]

## 5. 物理模型

每个10分钟时段满足：

\[
G_t^{use}+G_t^{em}+V_t^{use}+D_t=L_t+C_t,
\]

等价地写为：

\[
购电+紧急购电+光伏-弃光+放电=负荷+充电.
\]

储能状态方程为：

\[
SOC_{t+1}=SOC_t+0.9C_t-\frac{D_t}{0.9}.
\]

统一约束如下：

- `1200 ≤ SOC ≤ 10800 kWh`；
- 最大充电功率和放电功率均为5000 kW；
- 单个10分钟时段最大充电量和放电量均为 `5000/6=833.3333 kWh`；
- 计划购电、实际使用的合同电量和紧急购电均不得为负；
- 禁止向外部电网反向售电；
- 多余光伏优先充电，无法消纳时记为弃光；
- 使用MILP二进制状态禁止同一时段同时充电和放电；
- 目标函数加入 `1e-8×(充电量+放电量)` 的吞吐惩罚，仅用于消除无意义循环，不计入最终经济费用。

## 6. 问题4-2实现

问题4-2沿用问题2的因果预测与实时执行框架，主要修改为逐日读取附件4动态电价。

处理流程：

1. 每日0:00仅使用该日前的历史负荷和光伏；
2. 对普通滚动、周期衰减、RMSE最优和0.8分位数成本敏感预测进行比较；
3. 将预测净负荷输入具有完整未来价格曲线的储能优化模型；
4. 优化器可提前在低价时段充电，为未来高价时段保留电量；
5. 实际执行时优先使用实际光伏、合同电量和储能；
6. 实际供电仍不足时按当时动态电价的5倍购买紧急电量；
7. 最终预测模型按实际总购电费用选择。

问题4-2经济目标为：

\[
\min \sum_t c_tG_t^{plan}+5c_tG_t^{em}
+\lambda\sum_t(C_t+D_t).
\]

## 7. 问题4-3实现

问题4-3复用问题3的滚动MPC框架，并将静态电价替换为附件4对应日期的144点动态电价。

每日执行顺序：

1. 0:00根据0点光伏预测制定当天初始计划；
2. 先实际执行0:00—6:00，记录6:00真实SOC；
3. 6:00使用最新光伏预测和已观测负荷偏差，只优化6:00—24:00；
4. 同理在12:00、18:00更新尚未执行的后缀；
5. 已执行时段使用冻结断言保护，不允许被后续优化覆盖；
6. 每次优化均继承实际执行后的SOC；
7. 当日结束SOC作为下一日初始SOC继续传递。

调整费用严格区分方向：

\[
C_t^{down}=0.5c_t\max(G_t^{old}-G_t^{new},0),
\]

\[
C_t^{up}=1.5c_t\max(G_t^{new}-G_t^{old},0).
\]

总费用由初始计划费用、向上/向下调整费用和5倍电价紧急购电费用构成。

## 8. 光伏预测残差结果

残差定义为：

\[
e=实际光伏-预测光伏.
\]

统计范围为各发布时间至当日24:00的剩余可执行时域。

| 发布时间 | 样本数 | MAE/kW | RMSE/kW | 残差标准差/kW | 残差均值/kW |
|---|---:|---:|---:|---:|---:|
| 0:00 | 48,096 | 197.491 | 383.027 | 381.876 | 29.676 |
| 6:00 | 36,072 | 158.516 | 271.371 | 271.040 | 13.412 |
| 12:00 | 24,048 | 75.253 | 139.728 | 139.718 | -1.711 |
| 18:00 | 12,024 | 10.015 | 42.923 | 41.759 | -9.930 |

RMSE由0点的383.027 kW单调下降至18点的42.923 kW，说明预测时距缩短后精度显著提高，可作为采用多阶段滚动MPC的实证依据。

## 9. 三策略结果

策略A不使用日内动态价差信号进行优化，以输出期平均电价制定原策略，再按附件4实际动态电价重新结算，用作对照基线。

| 策略 | 总费用/元 | 计划费用/元 | 调整费用/元 | 紧急购电费/元 | 紧急电量/kWh | 弃光/kWh | 相对A节省 |
|---|---:|---:|---:|---:|---:|---:|---:|
| A 不考虑动态电价 | 17,644,414.51 | 17,371,159.69 | 0.00 | 273,254.82 | 61,102.81 | 1,727,424.45 | 0.000% |
| B 动态电价+储能优化 | 15,138,439.10 | 13,921,321.06 | 0.00 | 1,217,118.04 | 182,332.74 | 1,485,514.08 | 14.203% |
| C 动态电价+滚动MPC | 14,345,065.75 | 12,868,055.34 | 1,118,528.43 | 358,481.98 | 71,027.68 | 1,208,298.78 | 18.699% |

储能峰谷套利验证：

| 策略 | 充电加权电价/(元/kWh) | 放电加权电价/(元/kWh) | 放电－充电价差/(元/kWh) |
|---|---:|---:|---:|
| A | 0.6581 | 0.5075 | -0.1506 |
| B | 0.5089 | 1.0833 | 0.5744 |
| C | 0.5030 | 1.1187 | 0.6157 |

B、C策略的放电加权价格显著高于充电加权价格，说明模型确实利用了未来电价信息进行峰谷套利，而不是按当前价格贪心放空电池。

## 10. Excel输出说明

### 10.1 `result4-2.xlsx`

工作表保持为：

- `计划购电量`；
- `充放电量`；
- `紧急购电量`。

### 10.2 `result4-3.xlsx`

工作表保持为：

- `计划购电量`；
- `调整购电量`；
- `充放电量`；
- `紧急购电量`。

两个工作簿均已扩展至334天。计划和调整购电表为335行、147列，其中包含日期、144个时段、全天购电量和全天购电费。储能表按每日6个四小时区段汇总，紧急购电表按连续发生时段合并填写。

## 11. 最终审计结果

问题4-2和问题4-3均通过全部必需检查：

- 最大供需平衡误差：`2.274e-13 kWh`；
- 最大SOC递推误差：`9.095e-13 kWh`；
- 跨日SOC连续误差：`0 kWh`；
- MPC更新时间点SOC继承误差：`0 kWh`；
- 实际SOC最小值：`1200 kWh`；
- 实际SOC最大值：`10800 kWh`；
- 最大单时段充电量：`833.3333 kWh`；
- 最大单时段放电量：`833.3333 kWh`；
- 最大同时充放电量：`0 kWh`；
- 最小购电量：`0 kWh`；
- 已执行时段冻结：通过；
- 未来数据泄漏检查：通过；
- Excel公式错误扫描：0项；
- 每个工作表日期数：334天；
- 单元测试：49项全部通过。

## 12. 环境与运行方法

依赖见 `requirements.txt`：

```text
numpy>=1.26
scipy>=1.11
openpyxl>=3.1
```

在项目根目录执行：

```powershell
.\.venv\Scripts\python.exe .\problem4_optimization.py --root . --output42 .\result4-2.xlsx --output43 .\result4-3.xlsx
```

如需关闭多进程：

```powershell
.\.venv\Scripts\python.exe .\problem4_optimization.py --root . --no-parallel
```

运行完整测试：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

绘图模块优先使用 `matplotlib` 生成PNG；如果环境没有安装 `matplotlib`，会自动使用 `problem4_plotting.py` 生成SVG，不影响主程序完成。

## 13. 已知决策与后续注意事项

1. 问题4-2使用附件2和附件4进行优化；附件3只用于问题4整体残差分析和问题4-3，不进入问题4-2预测优化；
2. 策略A采用平均电价作为“无动态价格感知”基线，最终仍按附件4价格重算费用；论文中应明确这一定义；
3. 问题4-3采用机械执行0/6/12/18点更新，`event_triggered=False`，与题目要求的标准S3策略一致；代码保留事件触发接口，可用于扩展分析；
4. CVaR属于可选加分项，目前未强行纳入正式结果，因为尚未构造独立验证的预测误差场景集；
5. 吞吐惩罚仅作为数值择优项，最终报告费用不包含该惩罚；
6. 不要把附件列标签直接解释为区间起点，也不要对144列数据循环移位；
7. 若修改储能容量、效率或功率限制，应同步更新 `DispatchConfig`、论文公式、审计阈值和结果解释；
8. 若重新运行主程序，正式结果文件会按模板重新生成并覆盖同名文件，运行前应备份人工修改内容。

## 14. 论文建议表述

可将问题4的核心创新概括为：

> 通过残差分析量化多发布时间光伏预测误差，在动态电价环境下采用具备未来价格前瞻性的储能优化，并通过0:00、6:00、12:00、18:00多阶段MPC持续更新尚未执行的购电与储能决策，实现低价储能、高价释放，同时降低光伏预测误差引起的紧急购电风险。

